[package]
name = "stdlib-fs"
version = "0.1.0"

[build]
entry = "src/main.ax"
out_dir = "dist"

[capabilities]
fs = true
"fs:write" = true
net = false
process = false
env = false
clock = false
crypto = false
