import "std/env.ax"
import "std/time.ax"
import "server.ax"

let started: bool = now_ms() > 0

match get_env("AXIOM_TEST_BIND") {
Some(bind) {
print serve_health(bind, 1, started)
}
None {
print false
}
}
