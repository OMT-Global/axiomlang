pub fn headline(name: string): string {
return name + " cli"
}

pub fn describe(mode: string, profile: string, plan: string): string {
return "mode=" + mode + " profile=" + profile + " plan=" + plan
}
