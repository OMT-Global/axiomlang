[package]
name = "stdlib-env-unrestricted"
version = "0.1.0"

[build]
entry = "src/main.ax"
out_dir = "dist"

[capabilities]
fs = false
net = false
process = false
env_unrestricted = true
unsafe_rationale = "stdlib_env_unrestricted example intentionally exercises the migration escape hatch"
clock = false
crypto = false
