[package]
name = "stdlib-process"
version = "0.1.0"

[build]
entry = "src/main.ax"
out_dir = "dist"

[capabilities]
fs = false
net = false
process = true
env = false
clock = false
crypto = false
unsafe_rationale = "stdlib_process example exercises unrestricted process execution intentionally"
