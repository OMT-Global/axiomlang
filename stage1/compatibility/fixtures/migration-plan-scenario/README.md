# Migration plan scenario

These two contracts are synthetic inputs for the plan-only `axiomc migrate`
command. They exercise stable breaking, additive, and deprecation decisions.
Neither file describes a released, current, or previously supported compiler.
The real source-derived current contract lives in `../current/contract.json`.
