import "std/traits.ax"

struct Point {
x: int
}

impl Eq for Point {
fn eq(self, other: Point): bool {
return self.x == other.x
}
}

struct Box<T: Eq> {
item: T
}

let point: Point = Point { x: 3 }
let boxed: Box<Point> = Box { item: point }
print boxed.item.x
