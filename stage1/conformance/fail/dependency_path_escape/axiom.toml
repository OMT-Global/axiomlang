[package]
name = "conformance-dependency-path-escape"
version = "0.1.0"

[build]
entry = "src/main.ax"
out_dir = "dist"

[dependencies]
hello = { path = "../../../examples/hello" }

[capabilities]
fs = false
net = false
process = false
env = false
clock = false
crypto = false
