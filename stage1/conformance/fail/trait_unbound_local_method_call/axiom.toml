[package]
name = "conformance-trait-unbound-local-method-call"
version = "0.1.0"

[build]
entry = "src/main.ax"
out_dir = "dist"

[capabilities]
fs = false
net = false
process = false
env = false
clock = false
crypto = false

[expected_error]
kind = "type"
message = "method call \"eq\" on generic parameter \"T\" requires an explicit trait bound"
path = "src/main.ax"
line = 15
column = 8
